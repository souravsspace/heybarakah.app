diff --git a/node_modules/@bittingz/expo-widgets/.bun-tag-862064a093014a12 b/.bun-tag-862064a093014a12
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/plugin/build/ios/withPodfile.js b/plugin/build/ios/withPodfile.js
index e39bf98439ad763668d7e31dfc70e2ab2b600352..5c982625e97c9d4dedfdd23da8b6efd063a31081 100644
--- a/plugin/build/ios/withPodfile.js
+++ b/plugin/build/ios/withPodfile.js
@@ -45,33 +45,32 @@ const withPodfile = (config, options) => {
     const AppExtValue = AppExtAPIOnly ? 'YES' : 'No';
     const podFilePath = path.join(config.modRequest.platformProjectRoot, "Podfile");
     let podFileContent = fs.readFileSync(podFilePath).toString();
+    // Pure SwiftUI widget extension: no React Native / Expo pods. Linking them
+    // (use_react_native! / use_expo_modules!) integrates shared pods into both
+    // the app and the extension with mismatched Swift versions, which aborts
+    // pod install ("Unable to determine Swift version"). Empty target = no pods.
     const podInstaller = `
 target '${targetName}' do
-  use_expo_modules!
-  config = use_native_modules!
-
-  use_frameworks! :linkage => podfile_properties['ios.useFrameworks'].to_sym if podfile_properties['ios.useFrameworks']
-  use_frameworks! :linkage => ENV['USE_FRAMEWORKS'].to_sym if ENV['USE_FRAMEWORKS']
-
-  use_react_native!(
-    :path => config[:reactNativePath],
-    :hermes_enabled => podfile_properties['expo.jsEngine'] == nil || podfile_properties['expo.jsEngine'] == 'hermes',
-    # An absolute path to your application root.
-    :app_path => "#{Pod::Config.instance.installation_root}/..",
-    :privacy_file_aggregation_enabled => podfile_properties['apple.privacyManifestAggregationEnabled'] != 'false',
-  )
 end
 `;
-    const withAppExtFix = (0, generateCode_1.mergeContents)({
-        tag: "app_ext_fix",
-        src: podFileContent,
-        newSrc: `
+    let withAppExtFix;
+    try {
+        withAppExtFix = (0, generateCode_1.mergeContents)({
+            tag: "app_ext_fix",
+            src: podFileContent,
+            newSrc: `
         config.build_settings['APPLICATION_EXTENSION_API_ONLY'] = resource_bundle_target.name == 'Sentry' ? 'YES' : 'No'
         `,
-        anchor: /resource_bundle_target.build_configurations.each do \|config\|/,
-        offset: 1,
-        comment: "#",
-    });
+            anchor: /resource_bundle_target.build_configurations.each do \|config\|/,
+            offset: 1,
+            comment: "#",
+        });
+    }
+    catch (e) {
+        // Newer Expo Podfile templates (SDK 54+) omit the resource_bundle_target
+        // block this anchor targets; fix2's post_install patch covers it globally.
+        withAppExtFix = { contents: podFileContent };
+    }
     const withAppExtFixPt2 = (0, generateCode_1.mergeContents)({
         tag: 'fix2',
         src: withAppExtFix.contents,
diff --git a/plugin/src/ios/withPodfile.ts b/plugin/src/ios/withPodfile.ts
index 14583f4537b402e6d55f8919324d33f1b3c57f12..7decaddfb7fd3c2c3e8ab8de29e8af23bc3a5cb9 100644
--- a/plugin/src/ios/withPodfile.ts
+++ b/plugin/src/ios/withPodfile.ts
@@ -14,21 +14,12 @@ export const withPodfile = (config: ExportedConfigWithProps<XcodeProject>, optio
   const podFilePath = path.join(config.modRequest.platformProjectRoot, "Podfile");
   let podFileContent = fs.readFileSync(podFilePath).toString();

+  // Pure SwiftUI widget extension: no React Native / Expo pods. Linking them
+  // (use_react_native! / use_expo_modules!) integrates shared pods into both
+  // the app and the extension with mismatched Swift versions, which aborts
+  // pod install ("Unable to determine Swift version"). Empty target = no pods.
   const podInstaller = `
 target '${targetName}' do
-  use_expo_modules!
-  config = use_native_modules!
-
-  use_frameworks! :linkage => podfile_properties['ios.useFrameworks'].to_sym if podfile_properties['ios.useFrameworks']
-  use_frameworks! :linkage => ENV['USE_FRAMEWORKS'].to_sym if ENV['USE_FRAMEWORKS']
-
-  use_react_native!(
-    :path => config[:reactNativePath],
-    :hermes_enabled => podfile_properties['expo.jsEngine'] == nil || podfile_properties['expo.jsEngine'] == 'hermes',
-    # An absolute path to your application root.
-    :app_path => "#{Pod::Config.instance.installation_root}/..",
-    :privacy_file_aggregation_enabled => podfile_properties['apple.privacyManifestAggregationEnabled'] != 'false',
-  )
 end
 `;

diff --git a/plugin/build/ios/withWidgetXCode.js b/plugin/build/ios/withWidgetXCode.js
index d03cef2..4faecf7 100644
--- a/plugin/build/ios/withWidgetXCode.js
+++ b/plugin/build/ios/withWidgetXCode.js
@@ -146,6 +146,15 @@ const addFilesToWidgetProject = (project, { widgetFolderPath, iosProjectPath, ta
         else {
             // add to parent pbx group
             project.addToPbxGroup(pbxGroup.uuid, parentGroup.uuid);
+            // Upstream only compiles base-directory sources, so nested files
+            // (Widgets/, Shared/, Activity/, Controls/, ...) are added to groups
+            // but never to a Sources build phase -> "Cannot find X in scope".
+            // Add this directory's compilable files to the widget target too.
+            const nestedSources = [...filesByType.swift, ...filesByType.intentdefinition];
+            if (nestedSources.length > 0) {
+                logger_1.Logging.logger.debug(`Adding nested PBXSourcesBuildPhase for ${relativePath} to widget target ${widgetTargetUuid}`);
+                project.addBuildPhase(nestedSources, "PBXSourcesBuildPhase", groupName, widgetTargetUuid, "app_extension", "");
+            }
         }
         if (filesByType.xcassets) {
             for (const assetFile of filesByType.xcassets) {
diff --git a/plugin/src/ios/withWidgetXCode.ts b/plugin/src/ios/withWidgetXCode.ts
index f94333d..5c3f20a 100644
--- a/plugin/src/ios/withWidgetXCode.ts
+++ b/plugin/src/ios/withWidgetXCode.ts
@@ -222,6 +222,22 @@ const addFilesToWidgetProject = (
     else {
       // add to parent pbx group
       project.addToPbxGroup(pbxGroup.uuid, parentGroup.uuid)
+      // Upstream only compiles base-directory sources, so nested files
+      // (Widgets/, Shared/, Activity/, Controls/, ...) are added to groups
+      // but never to a Sources build phase -> "Cannot find X in scope".
+      // Add this directory's compilable files to the widget target too.
+      const nestedSources = [...filesByType.swift, ...filesByType.intentdefinition]
+      if (nestedSources.length > 0) {
+        Logging.logger.debug(`Adding nested PBXSourcesBuildPhase for ${relativePath} to widget target ${widgetTargetUuid}`)
+        project.addBuildPhase(
+          nestedSources,
+          "PBXSourcesBuildPhase",
+          groupName,
+          widgetTargetUuid,
+          "app_extension",
+          "",
+        )
+      }
     }

     if (filesByType.xcassets) {
